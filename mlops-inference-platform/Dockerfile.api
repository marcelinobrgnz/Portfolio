FROM python:3.11-slim

WORKDIR /app
ENV PYTHONDONTWRITEBYTECODE=1 \
    PYTHONUNBUFFERED=1 \
    MODEL_PATH=/app/models/wine_quality_model.pkl \
    MODEL_METADATA_PATH=/app/models/model_metadata.json

RUN apt-get update && apt-get install -y --no-install-recommends libgomp1 \
    && rm -rf /var/lib/apt/lists/*

COPY requirements.txt .
RUN pip install --no-cache-dir fastapi uvicorn[standard] pydantic joblib numpy scikit-learn xgboost python-dotenv

COPY src/ src/
COPY models/ models/

EXPOSE 8000
CMD ["uvicorn", "src.serve.main:app", "--host", "0.0.0.0", "--port", "8000"]
